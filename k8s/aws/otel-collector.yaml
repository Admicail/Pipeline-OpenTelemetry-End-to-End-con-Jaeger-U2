apiVersion: v1
kind: ConfigMap
metadata:
  name: otel-collector-config
  namespace: observability
data:
  config.yaml: |
    receivers:
      otlp:
        protocols:
          grpc:
            endpoint: 0.0.0.0:4317
          http:
            endpoint: 0.0.0.0:4318

    processors:
      batch:
      memory_limiter:
        check_interval: 1s
        limit_mib: 512
      resource:
        attributes:
          - action: insert
            key: deployment.environment
            value: aws
          - action: insert
            key: cloud.provider
            value: aws
          - action: insert
            key: cloud.region
            value: us-east-2
      filter/service-a:
        logs:
          include:
            match_type: strict
            resource_attributes:
              - key: service.name
                value: service-a
      filter/service-b:
        logs:
          include:
            match_type: strict
            resource_attributes:
              - key: service.name
                value: service-b

    exporters:
      otlp/jaeger:
        endpoint: jaeger:4317
        tls:
          insecure: true
      awsxray:
        region: us-east-2
      prometheus:
        endpoint: "0.0.0.0:8889"
      awscloudwatchlogs/service-a:
        log_group_name: "/otel/service-a/logs"
        log_stream_name: "service-a"
        region: us-east-2
      awscloudwatchlogs/service-b:
        log_group_name: "/otel/service-b/logs"
        log_stream_name: "service-b"
        region: us-east-2
      debug:
        verbosity: detailed

    service:
      pipelines:
        traces:
          receivers: [otlp]
          processors: [memory_limiter, resource, batch]
          exporters: [otlp/jaeger, awsxray]
        metrics:
          receivers: [otlp]
          processors: [memory_limiter, batch]
          exporters: [prometheus]
        logs/service-a:
          receivers: [otlp]
          processors: [memory_limiter, resource, filter/service-a, batch]
          exporters: [awscloudwatchlogs/service-a, debug]
        logs/service-b:
          receivers: [otlp]
          processors: [memory_limiter, resource, filter/service-b, batch]
          exporters: [awscloudwatchlogs/service-b, debug]
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: otel-collector
  namespace: observability
spec:
  replicas: 1
  selector:
    matchLabels:
      app: otel-collector
  template:
    metadata:
      labels:
        app: otel-collector
    spec:
      serviceAccountName: otel-collector
      containers:
        - name: otel-collector
          image: otel/opentelemetry-collector-contrib:0.103.0
          args: ["--config=/etc/otel/config.yaml"]
          volumeMounts:
            - name: config
              mountPath: /etc/otel
          ports:
            - containerPort: 4317
            - containerPort: 4318
            - containerPort: 8888
            - containerPort: 8889
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
      volumes:
        - name: config
          configMap:
            name: otel-collector-config
---
apiVersion: v1
kind: Service
metadata:
  name: otel-collector
  namespace: observability
spec:
  selector:
    app: otel-collector
  ports:
    - name: otlp-grpc
      port: 4317
      targetPort: 4317
    - name: otlp-http
      port: 4318
      targetPort: 4318
    - name: metrics-self
      port: 8888
      targetPort: 8888
    - name: metrics-prom
      port: 8889
      targetPort: 8889
  type: ClusterIP
